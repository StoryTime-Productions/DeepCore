package dev.deepcore.challenge;

/**
 * Prep GUI page identifiers.
 */
public enum PrepGuiPage {
    /** Top-level page showing prep categories. */
    CATEGORIES,
    /** Inventory-related settings page. */
    INVENTORY,
    /** Health-related settings page. */
    HEALTH,
    /** Run history/records page. */
    RUN_HISTORY
}
